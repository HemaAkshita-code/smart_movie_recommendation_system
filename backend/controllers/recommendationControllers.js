

async function recommend(req, res, next) 
{
    const FinalScore =
        // 0.60 * embeddingSimilarity
        // + 0.25 * featureSimilarity
        // + 0.10 * popularityBoost
        // + 0.05 * recencyBoost

        
}